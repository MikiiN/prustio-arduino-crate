# prustio-arduino-crate
The library crate for using the Arduino framework in PrustIO hybrid (Rust and C++) projects.

This crate is not created to be usable alone. It just provides the safe Rust interface for part of the Arduino framework. The entire compilation and linking process for the Arduino framework must be handled by an embedded application.   
